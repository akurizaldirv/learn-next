import React from 'react';

/*

    # default.tsx
    - is a special file that will load to nested route in slots

 */

const DefaultDashboardLayout = () => {
    return (
        <div>
            Dashboard
        </div>
    );
};

export default DefaultDashboardLayout;